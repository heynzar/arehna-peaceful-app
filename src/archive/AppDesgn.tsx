{
  /* <section className="flex bg-gradient-to-b from-[rgba(255,255,255,0.3)] to-transparent shadow-lg rounded-2xl w-full max-w-[400px] p-6 bg-clip-padding border border-white/40 backdrop-blur-[2px]  flex-col justify-center items-center text-center">
        <h1 className="text-[2.5rem] font-medium leading-10">THU 14 NOV</h1>
        <h2 className="text-8xl font-medium">12:33</h2>
      </section> */
}

{
  /* <section className="flex items-center w-full max-w-[400px] gap-2">
        <button className="button-gradient" aria-label="Play Quran sounds">
          <span className="text-lg font-medium">Quran</span>
          <Image src={audio} alt="Audio icon" width={24} height={24} />
        </button>

        <button className="button-gradient" aria-label="Play relaxing noise">
          <span className="text-lg font-medium">Noise</span>
          <Image src={audio} alt="Audio icon" width={24} height={24} />
        </button>
      </section> */
}

{
  /* <section className="flex items-center w-full max-w-[400px] gap-2">
        <button
          onClick={() => {
            setOpenQuran(true);
          }}
          className="w-full active:scale-95 flex items-center gap-2 bg-neutral-900/60 backdrop-blur-sm p-2 rounded-xl hover:bg-neutral-900/70 transition-all duration-300 justify-center"
          aria-label="Play Quran sounds"
        >
          <span className="text-lg">Quran</span>
          <Image src={audio} alt="Audio icon" width={24} height={24} />
        </button>

        <button
          onClick={() => {
            setOpenSounds(true);
          }}
          className="w-full active:scale-95 flex items-center gap-2 bg-neutral-900/60 backdrop-blur-sm p-2 rounded-xl hover:bg-neutral-900/70 transition-all duration-300 justify-center "
          aria-label="Play relaxing noise"
        >
          <span className="text-lg">Noise</span>
          <Image src={audio} alt="Audio icon" width={24} height={24} />
        </button>

        <button
          onClick={() => {
            setOpenSettings(true);
          }}
          className="w-min active:scale-95 flex items-center gap-2 bg-neutral-900/60 backdrop-blur-sm p-2 rounded-xl hover:bg-neutral-900/70 transition-all duration-300 justify-center "
          aria-label="Settings"
        >
          <Settings strokeWidth={1} />
        </button>
      </section> */
}
